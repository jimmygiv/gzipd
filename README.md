Made with the intent to read truncated zlib/gzip streams. I made this because I don't want to do it manually again :)


Usage:
  gzipd.py -d [base64 encoded zlib/gzip stream]
  gzipd.py -f [zlib/gzip compressed file/content]
